apiVersion: v1
kind: Service

metadata:
  name: simple-be

spec:
  selector:
    app: simple-be

  ports:
    - protocol: TCP
      port: {{ .Values.backend.service.port }}
      targetPort: 3000

  type: ClusterIP